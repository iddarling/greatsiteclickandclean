# 🧺 Click&Clean — CRM система для управления заказами

Простое приложение с фронтендом на **Vue 3 (Vite)** и сервером на **Node.js (Express + MongoDB)**.  
Позволяет просматривать, добавлять и управлять заказами, а также отображает современный интерфейс с адаптивным дизайном.

---

## 🚀 Структура проекта

```bash
N001_PRML/
├── backend/          ← Серверная часть (Node.js, Express, MongoDB)
│   ├── server.js
│   └── package.json
│
└── frontend/         ← Клиентская часть (Vue 3 + Vite)
    ├── src/
    │   ├── components/
    │   ├── views/
    │   ├── assets/
    │   └── main.ts
    └── package.json
⚙️ Требования
Node.js ≥ 18

npm ≥ 9

(опционально) MongoDB — локально или через MongoDB Atlas

🔧 Установка
Клонируйте репозиторий и перейдите в корень проекта:

bash
Копировать код
git clone https://github.com/ваш-профиль/ClickAndClean.git
cd N001_PRML
Установка зависимостей
Backend:
bash
Копировать код
cd backend
npm install
Frontend:
bash
Копировать код
cd ../frontend
npm install
▶️ Запуск проекта
1. Запуск сервера (Node.js)
bash
Копировать код
cd backend
npm run dev
Если nodemon не установлен — можно использовать node server.js.

Сервер будет доступен по адресу:
👉 http://localhost:3000

2. Запуск фронтенда (Vue 3)
Откройте новый терминал:

bash
Копировать код
cd frontend
npm run dev
После сборки откройте:
👉 http://localhost:5173

🌐 Прокси-настройка (для API)
Чтобы избежать CORS-ошибок, добавьте в frontend/vite.config.ts следующий код:

ts
Копировать код
export default {
  server: {
    proxy: {
      '/api': 'http://localhost:3000'
    }
  }
}
Теперь фронтенд сможет обращаться к API по пути /api/orders без указания полного URL.

🧩 Основные команды
Команда	Где запускать	Описание
npm run dev	backend	Запуск сервера с автоматическим перезапуском
npm start	backend	Обычный запуск сервера
npm run dev	frontend	Запуск фронтенда (Vite)
npm run build	frontend	Сборка фронтенда для продакшена
npm run preview	frontend	Предпросмотр собранного фронтенда

💡 Технологии
Frontend:

Vue 3

Vite

Pinia

Vue Router

Axios

Backend:

Express

CORS

Mongoose

dotenv

Database: MongoDB

Порты:

API — 3000

Frontend — 5173

🧠 Автор
Click&Clean CRM System
Разработано для учебного проекта по автоматизации службы выноса мусора.

yaml
Копировать код

---

Хочешь, я добавлю прямо в этот Markdown ещё блок:
> ⚙️ **Развёртывание проекта на Render и Vercel**  
чтобы ты мог выложить сайт онлайн (frontend + backend)?